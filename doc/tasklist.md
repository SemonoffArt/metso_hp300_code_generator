## Отчет по прогрессу

| Итерация | Описание                             | Статус   | Результат |
|----------|--------------------------------------|----------|-----------|
| 1        | Базовый скелет проекта и пустое UI   | 🟢 Done | Окно Tkinter создаётся и запускается через .venv |
| 2        | Связка UI с заглушкой алгоритма      | 🟢 Done | Поток ввод → STUB-расчёт → вывод в метку работает |
| 3        | Реализация реального алгоритма кода  | 🟢 Done | Алгоритм из algoritm.md реализован в core/calculator.py |
| 4        | Валидация ввода и сообщения об ошибках | 🟢 Done | Некорректный ввод даёт сообщение, приложение не падает |
| 5        | Минимальная упаковка и запуск из окружения uv | 🟢 Done | Команды запуска через uv зафиксированы в vision.md |

> Статусы: ⬜ Planned · 🟡 In progress · 🟢 Done · 🔴 Blocked

---

## План итераций

### Итерация 1. Базовый скелет проекта и пустое UI

- [x] Создать структуру проекта в соответствии с `doc/vision.md` (минимум: `main.py`, каталог `doc/`).
- [x] Инициализировать проект и окружение с помощью `uv` (без дополнительных зависимостей).
- [x] Реализовать минимальное Tkinter-окно: заголовок приложения, поле ввода и кнопка без логики.
- [x] Проверить, что приложение запускается командой `python main.py` и окно отображается без ошибок.

### Итерация 2. Связка UI с заглушкой алгоритма

- [x] Добавить функцию расчета кода в отдельном модуле/функции (пока простая заглушка, например, возвращает фиксированное значение или простое преобразование).
- [x] Подключить эту функцию к кнопке в Tkinter: при нажатии брать код из поля ввода и отображать результат в метке.
- [x] Убедиться, что полный поток "ввод → расчет (заглушка) → вывод" работает без ошибок.

### Итерация 3. Реализация реального алгоритма генерации кода

- [x] Заменить заглушку в модуле расчета на реальную реализацию алгоритма (алгоритм описан в файле @algoritm.md).
- [x] Протестировать несколько реальных примеров кодов, убедиться в корректности результатов.

### Итерация 4. Валидация ввода и сообщения об ошибках

- [x] Добавить проверку формата и длины вводимого кода (минимально необходимая логика для реальных кейсов).
- [x] Реализовать понятные сообщения об ошибках в UI (например, при пустом поле или неверном формате).
- [x] Проверить, что некорректный ввод не приводит к падению приложения и пользователь видит понятное сообщение.

### Итерация 5. Минимальная упаковка и запуск из окружения uv

- [x] Описать в `doc/README` или `doc/vision.md` минимальные команды для установки/запуска через `uv`.
- [x] Убедиться, что приложение запускается в чистом окружении, созданном `uv`, только со стандартными зависимостями.
- [ ] (Опционально) Подготовить простой скрипт или команду для сборки одного исполняемого файла с помощью PyInstaller для будущего использования.
